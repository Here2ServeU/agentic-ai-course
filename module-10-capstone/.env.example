# Copy this file to ".env" and fill in your real values.
# NEVER commit your real .env — it is git-ignored for a reason.
#
# Load these into your shell before running, e.g.:
#   Mac/Linux:   export $(grep -v '^#' .env | xargs)
#   or set each one with `export NAME=value`

OPENAI_API_KEY=sk-your-openai-key-here

# Observability (Module 9) — free account at https://smith.langchain.com
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your-langsmith-key-here
LANGCHAIN_PROJECT=capstone-fraud-agent
